#only in windows
alias exp='explorer.exe .' 
alias pop="explorer ."

alias g11='g++ -std=c++11'
alias e='echo $?'

# https://stackoverflow.com/questions/40974170/how-to-ignore-line-endings-when-comparing-files
alias diff="diff --strip-trailing-cr"


#show certain line of a file 

#fix .bashrc don't work, add following to .bash_profile
#if [ -f ~/.bashrc ]; then
#. ~/.bashrc
#fi
